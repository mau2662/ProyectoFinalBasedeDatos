@model IEnumerable<ProyectoLenguajesNetCore.Models.ALMACEN>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NOMBRE_ESTANTE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NOMBRE_ESTANTE)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_ESTANTE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_ESTANTE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_ESTANTE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
